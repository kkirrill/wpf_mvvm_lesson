<Window x:Class="wpf_mvvm_lesson.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:wpf_mvvm_lesson.ViewModels"
        xmlns:local="clr-namespace:wpf_mvvm_lesson"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <TextBox Margin="30,30, 500, 330" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

    </Grid>
</Window>
